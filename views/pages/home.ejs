<!DOCTYPE html>
<html lang="en">
  <head>
     <% include ../partials/ldOut %>
  </head>
  <body class="slds">
    <header>
    </header>
    <main>
      <div >
        <p>Sales Leaderboard On Heroku</p>
        <div id="leaderboard"></div>
          </div>
      </main>
        <footer>
        </footer>
        <script>
          var url = "<%= ouathLightningURL %>";
          var token = "Bearer "+"<%= oauthtoken %>";
          console.log(token);
            $Lightning.use("c:OppLightningWebCompApp", function() {
                  $Lightning.createComponent("c:opportunityList", {},
                     "leaderboard",
                      function(cmp) {
                         alert('Lightning Out App has loaded!');
                      });
            },url,token );
        </script>
  </body>
</html>
